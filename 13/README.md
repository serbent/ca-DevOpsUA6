# 13. Refresh memory

## Task.

Create a script that would have a few functions.  
Create a docker image based on nginx image that would host a simple webpage. (see code at the bottom) 
Rewuirements for the script:  
* Run every minute 
* Check if Dockerfile was changed if so build it and re-run the container. 
* Check if the container is running. 
* Provide run logs of the script. 




```html
<html>
    <title>My Perfect First deployment</title>
    <body>
        This is some webpage that i've created.
    </body>
</html>
```
