# 13. Refresh memory

## Task.

Create a script that would have a few functions.   
Rewuirements for the script:  
* Run every minute 
* Check if Dockerfile was changed if so build it and re-run the container. 
* Check if the container is running. 
* Provide run logs of the script. 